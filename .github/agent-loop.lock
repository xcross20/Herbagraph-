# Founder-approved trusted orchestrator SHA on protected main.
# Empty is fail-closed: the loop will not activate until a founder sets a
# 40-character SHA here. Descendants of that SHA are accepted via GitHub compare.
approved_bootstrap_sha=
